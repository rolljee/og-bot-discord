# Copy this file to scripts/.env and fill in the values.
# scripts/.env is git-ignored and must never be committed.

# Discord bot token (Discord Developer Portal > your app > Bot > Reset Token)
DISCORD_TOKEN=

# Deploy target (only needed for `npm run deploy`)
USER=
IP_ADDR=
